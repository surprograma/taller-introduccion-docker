{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\""
  },
  "dependencies": {
    "reveal.js": "^4.1.0"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "snowpack": "^3.0.1"
  }
}
